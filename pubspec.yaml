name: top_alert_notification
description: Creates a top notification centered in the screen. Pass in a child widget, a function for onTap, and enable a dismiss button. Supports a swipe up to dismiss option.
version: 0.0.4
homepage: https://github.com/jessecanderson/top_alert_notification

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
